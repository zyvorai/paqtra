# ─── Cilium Vision — Terraform Configuration Example ─────────
# Copy to terraform.tfvars and customize

aws_region         = "us-west-2"
cluster_name       = "cilium-vision-cluster"
kubernetes_version = "1.29"
environment        = "dev"

# Node configuration
node_instance_type = "t3.medium"
node_min_count     = 2
node_max_count     = 6
node_desired_count = 3

# Cilium
install_cilium    = true
cilium_version    = "1.15.3"
enable_encryption = false

# Cilium Vision
namespace            = "cilium-system"
api_image_repository = "ghcr.io/ssahani/cilium-flow-api"
api_image_tag        = "latest"
api_replicas         = 2
ui_replicas          = 2

# Ingress (optional)
enable_ingress = false
ingress_hosts  = ["cilium-vision.example.com"]

# Monitoring (optional)
enable_monitoring = false

tags = {
  Project     = "cilium-vision"
  Environment = "dev"
  ManagedBy   = "terraform"
}
