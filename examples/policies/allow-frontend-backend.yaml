# Example CiliumNetworkPolicy: Allow frontend to backend
# Generated by Cilium Vision AutoPolicy with 95% ML confidence
apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: allow-frontend-backend
  namespace: default
  annotations:
    cilium-vision.io/generated: "true"
    cilium-vision.io/confidence: "95.2"
    cilium-vision.io/learning-duration: "7d"
    cilium-vision.io/observations: "1247"
spec:
  endpointSelector:
    matchLabels:
      app: backend
      tier: api
  ingress:
  - fromEndpoints:
    - matchLabels:
        app: frontend
        tier: web
    toPorts:
    - ports:
      - port: "8080"
        protocol: TCP
      rules:
        http:
        - method: "GET"
          path: "/api/.*"
        - method: "POST"
          path: "/api/.*"
