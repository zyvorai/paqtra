# Example CiliumNetworkPolicy: DNS egress
# Allow all pods to reach DNS servers
apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: dns-egress-policy
  namespace: default
  annotations:
    cilium-vision.io/generated: "true"
    cilium-vision.io/confidence: "98.7"
    cilium-vision.io/description: "Allow DNS queries to cluster DNS and public DNS"
spec:
  endpointSelector: {}
  egress:
  - toEndpoints:
    - matchLabels:
        k8s:io.kubernetes.pod.namespace: kube-system
        k8s-app: kube-dns
    toPorts:
    - ports:
      - port: "53"
        protocol: UDP
      rules:
        dns:
        - matchPattern: "*"
  - toFQDNs:
    - matchName: "dns.google"
    - matchName: "cloudflare-dns.com"
    toPorts:
    - ports:
      - port: "53"
        protocol: UDP
